const typing = document.getElementById(typing);

function type() {
    const typeWords = ['Clear,', 'Functional,', 'Beautiful', ];
    const typeLetters2 = 'Designs that speak for you'; 
    const wordsTyped = [];

    if(i < typeWords.lastIndexOf;)


    typing.innerHTML = `${wordsTyped}`;
}

type()